# Frontend env — `apps/web/.env.local` da nusxa qiling.
#
# Hozircha frontend `/api` ni hardcoded ishlatadi:
#   - Dev: vite.config.ts proxy → http://localhost:8000
#   - Prod: Edge Nginx proxy → API container :8000
#
# VITE_API_URL kelajakda mutlaq URL kerak bo'lganda qo'shiladi.
